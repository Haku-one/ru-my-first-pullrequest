# Руководство по установке карты СДЭК для WooCommerce

## Описание

Данное решение добавляет интерактивную карту с пунктами выдачи СДЭК на страницу оформления заказа в WooCommerce. Пользователи смогут выбирать пункты выдачи на карте Яндекс.Карт, как это делается в современных сервисах доставки.

## Возможности

- 🗺️ Интерактивная карта с пунктами выдачи СДЭК
- 📍 Автоматическое определение города по IP или адресу пользователя
- 🔍 Поиск пунктов выдачи по городу
- 📱 Адаптивный дизайн для мобильных устройств
- ⚡ Кэширование данных для быстрой работы
- 🔒 Безопасная интеграция с WordPress REST API
- 🎨 Современный дизайн в стиле WooCommerce

## Требования

- WordPress 5.0+
- WooCommerce 4.0+
- PHP 7.4+
- Плагин интеграции с СДЭК (например, "СДЭК" или аналогичный)
- API ключи СДЭК
- API ключ Яндекс.Карт (рекомендуется)

## Установка

### Шаг 1: Копирование файлов

1. Скопируйте следующие файлы в папку вашей темы WordPress или создайте плагин:

```
/wp-content/themes/your-theme/
├── cdek-api-handler.php
├── cdek-map-integration-updated.js
├── cdek-map-styles.css
```

Или создайте отдельный плагин:

```
/wp-content/plugins/cdek-map/
├── cdek-map.php (главный файл плагина)
├── includes/
│   ├── class-cdek-api-handler.php
│   ├── assets/
│   │   ├── js/cdek-map-integration.js
│   │   └── css/cdek-map-styles.css
```

### Шаг 2: Подключение файлов

Если вы используете тему, добавьте в `functions.php`:

```php
// Подключаем обработчик API СДЭК
require_once get_template_directory() . '/cdek-api-handler.php';

// Подключаем стили и скрипты
function cdek_enqueue_assets() {
    if (is_checkout()) {
        wp_enqueue_style(
            'cdek-map-styles',
            get_template_directory_uri() . '/cdek-map-styles.css',
            array(),
            '1.0.0'
        );
        
        wp_enqueue_script(
            'cdek-map-integration',
            get_template_directory_uri() . '/cdek-map-integration-updated.js',
            array('jquery'),
            '1.0.0',
            true
        );
    }
}
add_action('wp_enqueue_scripts', 'cdek_enqueue_assets');
```

### Шаг 3: Создание плагина (рекомендуется)

Создайте файл `cdek-map.php`:

```php
<?php
/**
 * Plugin Name: СДЭК Карта пунктов выдачи
 * Description: Добавляет интерактивную карту с пунктами выдачи СДЭК на страницу checkout
 * Version: 1.0.0
 * Author: Ваше имя
 */

// Предотвращаем прямой доступ
if (!defined('ABSPATH')) {
    exit;
}

// Константы плагина
define('CDEK_MAP_PLUGIN_URL', plugin_dir_url(__FILE__));
define('CDEK_MAP_PLUGIN_PATH', plugin_dir_path(__FILE__));

// Подключаем основной класс
require_once CDEK_MAP_PLUGIN_PATH . 'includes/class-cdek-api-handler.php';

// Инициализация плагина
function cdek_map_init() {
    new CDEK_API_Handler();
}
add_action('plugins_loaded', 'cdek_map_init');

// Активация плагина
register_activation_hook(__FILE__, 'cdek_map_activate');
function cdek_map_activate() {
    // Создаем настройки по умолчанию
    add_option('cdek_client_id', '');
    add_option('cdek_client_secret', '');
    add_option('cdek_yandex_api_key', '');
}

// Деактивация плагина
register_deactivation_hook(__FILE__, 'cdek_map_deactivate');
function cdek_map_deactivate() {
    // Очищаем кэш
    delete_transient('cdek_access_token');
}
```

## Настройка

### Шаг 1: Получение API ключей СДЭК

1. Зарегистрируйтесь в [личном кабинете СДЭК](https://www.cdek.ru/ru/integration)
2. Перейдите в раздел "Интеграция" → "API"
3. Создайте новое приложение
4. Получите `Client ID` и `Client Secret`

### Шаг 2: Получение API ключа Яндекс.Карт

1. Перейдите на [Developer Яндекс](https://developer.tech.yandex.ru/)
2. Зарегистрируйтесь и создайте новое приложение
3. Выберите "JavaScript API и HTTP Геокодер"
4. Получите API ключ

### Шаг 3: Настройка в WordPress

1. Перейдите в админку WordPress
2. Откройте "Настройки" → "СДЭК"
3. Введите полученные API ключи:
   - Client ID СДЭК
   - Client Secret СДЭК
   - API ключ Яндекс.Карт
4. Сохраните настройки

### Шаг 4: Настройка темы checkout

Убедитесь, что в вашем шаблоне checkout есть блок для карты:

```html
<div class="wp-block-cdek-checkout-map-block"></div>
```

Если его нет, добавьте в соответствующий шаблон темы или с помощью хука:

```php
// Добавляем блок карты после методов доставки
add_action('woocommerce_checkout_after_shipping', 'add_cdek_map_block');
function add_cdek_map_block() {
    echo '<div class="wp-block-cdek-checkout-map-block"></div>';
}
```

## Кастомизация

### Изменение стилей

Вы можете переопределить стили, добавив CSS в вашу тему:

```css
/* Изменение цветовой схемы */
.cdek-map-search button {
    background: #your-color !important;
}

.cdek-select-office-btn {
    background: #your-color !important;
}

/* Изменение размеров карты */
#cdek-yandex-map {
    height: 500px !important;
}
```

### Изменение логики

Вы можете расширить функциональность, модифицируя JavaScript:

```javascript
// Добавление собственных обработчиков
document.addEventListener('cdek:officeSelected', function(e) {
    const office = e.detail.office;
    // Ваша логика
});

// Изменение тестовых данных
CDEKMapIntegration.prototype.getTestOffices = function() {
    return [
        // Ваши тестовые офисы
    ];
};
```

## Отладка

### Проверка API

1. Откройте консоль разработчика в браузере
2. Перейдите на страницу checkout
3. Проверьте вкладку "Network" на наличие запросов к `/wp-json/cdek/v1/offices`
4. Проверьте вкладку "Console" на наличие ошибок JavaScript

### Распространенные проблемы

**Карта не загружается:**
- Проверьте API ключ Яндекс.Карт
- Убедитесь, что домен добавлен в настройки API ключа

**Пункты выдачи не загружаются:**
- Проверьте API ключи СДЭК
- Убедитесь, что сервер может делать исходящие HTTP запросы
- Проверьте логи WordPress на наличие ошибок

**Карта не отображается на checkout:**
- Проверьте наличие блока `wp-block-cdek-checkout-map-block`
- Убедитесь, что JavaScript файл подключается на странице checkout

### Логирование

Для отладки добавьте в `wp-config.php`:

```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```

И в код обработчика API:

```php
error_log('CDEK API Response: ' . print_r($response, true));
```

## Безопасность

### Рекомендации

1. **Используйте HTTPS** для всех API запросов
2. **Ограничьте домены** в настройках API ключей
3. **Регулярно обновляйте** API ключи
4. **Проверяйте входящие данные** перед обработкой
5. **Используйте nonce** для защиты от CSRF атак

### Настройка CORS

Если возникают проблемы с CORS, добавьте в `.htaccess`:

```apache
Header always set Access-Control-Allow-Origin "*"
Header always set Access-Control-Allow-Methods "GET, POST, OPTIONS"
Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-WP-Nonce"
```

## Производительность

### Оптимизация

1. **Кэширование API токенов** - уже реализовано
2. **Минификация CSS/JS** - используйте плагины минификации
3. **CDN для статических ресурсов** - настройте CDN
4. **Оптимизация изображений** - сжимайте изображения карт

### Мониторинг

Отслеживайте производительность с помощью:
- WordPress Query Monitor
- New Relic
- Google PageSpeed Insights

## Поддержка

### Обновления

Для получения обновлений:
1. Следите за изменениями в API СДЭК
2. Обновляйте API ключи при необходимости
3. Тестируйте функциональность после обновлений WordPress/WooCommerce

### Документация API

- [API СДЭК](https://api-docs.cdek.ru/)
- [Яндекс.Карты API](https://yandex.ru/dev/maps/jsapi/)
- [WooCommerce REST API](https://woocommerce.github.io/woocommerce-rest-api-docs/)

## Лицензия

Данный код предоставляется "как есть" без каких-либо гарантий. Используйте на свой страх и риск.

---

**Автор:** Создано для улучшения пользовательского опыта в интернет-магазинах с доставкой СДЭК.